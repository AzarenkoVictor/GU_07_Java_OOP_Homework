package Units;

public class Crossbowman extends Archer {
     public Crossbowman(String name, int level, int maxHp, int baseDamage, int baseProtection, int arrows) {
          super(name, level, maxHp, baseDamage, baseProtection, arrows);
     }
}
